{
  int b=getBearing();
  if (distChanged(cachedDegrees,b)) {
    cachedDegrees=b;
    if (b >= 0) {
      setText(String.valueOf(b) + "??",null);
    }
 else {
      setText(null,null);
    }
    return true;
  }
  return false;
}
