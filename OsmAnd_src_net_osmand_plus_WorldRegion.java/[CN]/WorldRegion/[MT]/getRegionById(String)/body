{
  if (regionId == null) {
    return this;
  }
 else {
    for (    WorldRegion region : flattenedSubregions) {
      if (region.getRegionId().equals(regionId)) {
        return region;
      }
    }
  }
  return null;
}
