{
  String highway=null;
  int sz=types.size();
  for (int i=0; i < sz; i++) {
    RouteTypeRule r=region.quickGetEncodingRule(types.getQuick(i));
    highway=r.highwayRoad();
    if (highway != null) {
      break;
    }
  }
  return highway;
}
