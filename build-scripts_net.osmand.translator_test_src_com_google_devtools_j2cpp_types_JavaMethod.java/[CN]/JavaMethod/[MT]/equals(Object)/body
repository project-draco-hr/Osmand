{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof JavaMethod)) {
    return false;
  }
  JavaMethod other=(JavaMethod)obj;
  return clazz.equals(other.clazz) && name.equals(other.name) && signature.equals(other.signature);
}
