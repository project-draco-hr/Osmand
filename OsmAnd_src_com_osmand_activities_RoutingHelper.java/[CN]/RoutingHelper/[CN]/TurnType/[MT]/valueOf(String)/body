{
  for (  TurnType v : vals) {
    if (v.getValue().equals(s)) {
      return v;
    }
  }
  if (s != null && s.startsWith("EXIT")) {
    return getExitTurn(Integer.parseInt(s.substring(4)));
  }
  return null;
}
