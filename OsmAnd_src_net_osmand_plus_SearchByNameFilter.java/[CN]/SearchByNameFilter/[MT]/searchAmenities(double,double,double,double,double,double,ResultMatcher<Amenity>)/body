{
  searchedAmenities.clear();
  searchedAmenities=application.getResourceManager().searchAmenitiesByName(query,topLatitude,leftLongitude,bottomLatitude,rightLongitude,lat,lon,matcher);
  MapUtils.sortListOfMapObject(searchedAmenities,lat,lon);
  return searchedAmenities;
}
