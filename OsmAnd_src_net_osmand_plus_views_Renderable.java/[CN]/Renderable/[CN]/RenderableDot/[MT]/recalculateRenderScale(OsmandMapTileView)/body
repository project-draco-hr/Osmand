{
  if (culled == null && culler == null) {
    culler=new AsynchronousResampler(renderType,view,this,param1,param2);
    assert(culler != null);
    if (culler != null)     culler.execute("");
  }
}
