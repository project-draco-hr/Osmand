{
  if (zoom != newZoom) {
    if (culler != null) {
      culler.cancel(true);
    }
    culled.clear();
    zoom=newZoom;
    double length=Math.pow(2.0,17 - zoom) * segmentSize;
    culler=getCuller(length);
    culler.execute("");
  }
}
