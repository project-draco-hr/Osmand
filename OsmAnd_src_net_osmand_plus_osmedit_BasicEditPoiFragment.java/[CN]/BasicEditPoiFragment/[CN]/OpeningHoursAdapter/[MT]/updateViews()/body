{
  linearLayout.removeAllViews();
  for (int i=0; i < openingHours.getRules().size(); i++) {
    linearLayout.addView(getView(i));
  }
  if (!data.isInEdit()) {
    data.putTag(OSMSettings.OSMTagKey.OPENING_HOURS.getValue(),openingHours.toStringNoMonths());
  }
}
