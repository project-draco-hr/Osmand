{
  return c.prepareStatement("SELECT A.id, A.name, A.name_en, A.latitude, A.longitude, " + "B.id, B.name, B.name_en, B.latitude, B.longitude, B.postcode " + "FROM street A INNER JOIN building B ON B.street = A.id WHERE A.city = ? ");
}
