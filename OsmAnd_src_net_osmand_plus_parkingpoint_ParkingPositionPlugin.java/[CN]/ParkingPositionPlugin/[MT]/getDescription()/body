{
  StringBuilder res=new StringBuilder();
  res.append(app.getString(R.string.osmand_parking_plugin_description));
  if (settings.getParkingType())   res.append(settings.getParkingHour());
  res.append(" ");
  res.append(settings.getParkingMinute());
  return res.toString();
}
