{
  Statement stat=conn.createStatement();
  stat.executeUpdate("create table route_objects (id bigint primary key, " + "types binary, pointTypes binary, pointIds binary, pointCoordinates binary)");
  stat.executeUpdate("create index route_objects_ind on binary_map_objects (id)");
  stat.close();
}
