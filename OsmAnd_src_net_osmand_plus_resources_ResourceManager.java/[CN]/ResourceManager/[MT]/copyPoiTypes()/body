{
  try {
    File file=context.getAppPath("poi_types.xml");
    if (file != null) {
      Algorithms.streamCopy(MapPoiTypes.class.getResourceAsStream("poi_types.xml"),new FileOutputStream(file));
    }
  }
 catch (  Exception e) {
    log.error(e.getMessage(),e);
  }
}
