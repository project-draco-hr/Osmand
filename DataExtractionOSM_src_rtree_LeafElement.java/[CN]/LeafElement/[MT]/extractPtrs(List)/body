{
  if (elements == null)   throw new IllegalArgumentException("RTree.LeafElement: Argument null");
  List result=new ArrayList();
  try {
    for (Iterator i=elements.iterator(); i.hasNext(); )     result.add(new Integer((int)((LeafElement)i.next()).getPtr()));
    return result;
  }
 catch (  ClassCastException e) {
    throw new IllegalArgumentException("RTree.LeafElement: Type of vector is not LeafElement");
  }
}
