{
  for (  MapWidgetRegInfo ri : left) {
    if (cl.isInstance(ri)) {
      return (T)ri.widget;
    }
  }
  for (  MapWidgetRegInfo ri : right) {
    if (cl.isInstance(ri)) {
      return (T)ri.widget;
    }
  }
  return null;
}
