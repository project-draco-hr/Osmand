{
  for (  MapWidgetRegInfo ri : leftWidgetSet) {
    if (cl.isInstance(ri)) {
      return (T)ri.widget;
    }
  }
  for (  MapWidgetRegInfo ri : rightWidgetSet) {
    if (cl.isInstance(ri)) {
      return (T)ri.widget;
    }
  }
  return null;
}
