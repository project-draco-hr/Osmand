{
  super.onResume();
  favouritesAdapter.synchronizeGroups();
  favouritesAdapter.sort(favoritesComparator);
  if (favouritesAdapter.getGroupCount() > 0 && "".equals(favouritesAdapter.getGroup(0))) {
    getListView().expandGroup(0);
  }
}
