{
  super.onResume();
  favouritesAdapter.synchronizeGroups();
  if (favouritesAdapter.getGroupCount() > 0 && "".equals(favouritesAdapter.getGroup(0).name)) {
    getExpandableListView().expandGroup(0);
  }
}
