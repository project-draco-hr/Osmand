{
  Context context=originalView.getContext();
  final android.support.v7.view.ActionMode supportActionMode=startSupportActionMode(new SupportActionModeWrapper.CallbackWrapper(context,callback));
  if (supportActionMode != null) {
    return new SupportActionModeWrapper(mContext,supportActionMode);
  }
  return null;
}
