{
  service=new OsMoService(app);
  tracker=new OsMoTracker(service,app.getSettings().OSMO_SAVE_TRACK_INTERVAL,app.getSettings().OSMO_AUTO_SEND_LOCATIONS);
  this.app=app;
  ApplicationMode.regWidget("osmo_control",(ApplicationMode[])null);
}
