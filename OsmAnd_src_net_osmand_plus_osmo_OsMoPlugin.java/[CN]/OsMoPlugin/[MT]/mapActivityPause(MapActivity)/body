{
  mv=null;
  tracker.setUITrackerListener(new OsmoTrackerListener(){
    @Override public void locationChange(    String trackerId,    Location location){
      if (mv != null) {
        mv.refreshMap();
      }
    }
  }
);
}
