{
  searchAPI.setObfAreaFilter(bounds31);
  searchAPI.startSearch(keyword,MAX_SEARCH_RESULTS_CORE,new SearchCallback(){
    @Override public void onSearchFinished(    List<SearchItem> searchItems){
      processSearchResult(searchItems);
    }
  }
,new SearchCallback(){
    @Override public void onSearchFinished(    List<SearchItem> searchItems){
      processSearchResult(searchItems);
    }
  }
);
}
