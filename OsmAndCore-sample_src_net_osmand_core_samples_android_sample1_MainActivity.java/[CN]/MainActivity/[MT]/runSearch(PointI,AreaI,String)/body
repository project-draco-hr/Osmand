{
  searchAPI.setSearchLocation31(position31);
  searchAPI.setObfAreaFilter(bounds31);
  searchAPI.startSearch(keyword,MAX_SEARCH_RESULTS_CORE,new SearchApiCallback(){
    @Override public void onSearchFinished(    List<SearchObject> searchObjects){
      processSearchResult(searchObjects);
    }
  }
,new SearchApiCallback(){
    @Override public void onSearchFinished(    List<SearchObject> searchObjects){
      processSearchResult(searchObjects);
    }
  }
);
}
