{
  super(context);
  application=(OsmandApplication)context.getApplicationContext();
  currentViewport=new RotatedTileBox.RotatedTileBoxBuilder().setLocation(0,0).setZoomAndScale(3,1).build();
  initView();
}
