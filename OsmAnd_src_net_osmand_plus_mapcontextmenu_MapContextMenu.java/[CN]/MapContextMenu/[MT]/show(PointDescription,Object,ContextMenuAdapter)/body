{
  if (isMenuVisible())   hide();
  this.pointDescription=pointDescription;
  this.object=object;
  this.menuAdapter=menuAdapter;
  acquireStretName();
  MapContextMenuFragment.showInstance(mapActivity);
}
