{
  super.onResume();
  setContent();
  selectedGpxHelper.setUiListener(SelectedGPXFragment.class,new Runnable(){
    @Override public void run(){
      adapter.setDisplayGroups(selectedGpxHelper.getDisplayGroups());
    }
  }
);
}
