{
  if (visibleType == DashboardType.WAYPOINTS || visibleType == DashboardType.WAYPOINTS_EDIT || visibleType == DashboardType.CONFIGURE_MAP || force) {
    updateListAdapter();
  }
 else {
    listAdapter.notifyDataSetChanged();
  }
}
