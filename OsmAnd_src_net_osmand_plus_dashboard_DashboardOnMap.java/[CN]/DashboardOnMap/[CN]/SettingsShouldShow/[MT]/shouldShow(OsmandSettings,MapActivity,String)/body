{
  Log.v(TAG,"shouldShow(" + "settings=" + settings + ", activity="+ activity+ ", tag="+ tag+ ")");
  boolean shouldShow=settings.registerBooleanPreference(SHOULD_SHOW + tag,true).makeGlobal().get();
  Log.v(TAG,"shouldShow=" + shouldShow);
  return shouldShow;
}
