{
  result=null;
  workerThread=new WorkerThread();
  workerThread.start();
  setVisible(true);
  if (workerThread.checkIsLive()) {
    workerThread.stop();
    throw new InterruptedException();
  }
  if (exception != null) {
    throw exception;
  }
  return result;
}
