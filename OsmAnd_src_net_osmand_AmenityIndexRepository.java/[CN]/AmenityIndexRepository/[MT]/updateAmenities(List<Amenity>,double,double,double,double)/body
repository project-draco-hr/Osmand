{
  db.execSQL("DELETE FROM " + IndexConstants.POI_TABLE + " WHERE "+ " longitude >= ? AND ? >= longitude  AND "+ " latitude >= ? AND ? >= latitude ",new Double[]{leftLon,rightLon,bottomLat,topLat});
  insertAmenities(amenities);
  return true;
}
