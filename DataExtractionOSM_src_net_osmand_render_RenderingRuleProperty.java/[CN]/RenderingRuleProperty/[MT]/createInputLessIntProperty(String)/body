{
  return new RenderingRuleProperty(name,INT_TYPE,true){
    @Override public boolean accept(    int ruleValue,    int renderingProperty){
      if ((type != INT_TYPE || type != STRING_TYPE) && input) {
        return false;
      }
      return ruleValue >= renderingProperty;
    }
  }
;
}
