{
  super.onLayout(changed,left,top,right,bottom);
  Rect padding=new Rect();
  if (background.getPadding(padding)) {
    background.setBounds(-padding.left + left,top - padding.top,right + padding.right,bottom + padding.bottom);
  }
}
