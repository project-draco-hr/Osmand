{
  int w=0;
  if (text != null) {
    w+=textPaint.measureText(text) + 3 * scaleCoefficient;
    if (subtext != null) {
      w+=textPaint.measureText(subtext) + 4 * scaleCoefficient;
    }
  }
  return w;
}
