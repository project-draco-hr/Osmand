{
  super.onDraw(cv);
  if (isVisible()) {
    cv.drawText(text,2 * scaleCoefficient,getHeight() - scaleCoefficient,textPaint);
    if (subtext != null) {
      cv.drawText(subtext,4 * scaleCoefficient + textPaint.measureText(text),getHeight() - scaleCoefficient,subtextPaint);
    }
  }
}
