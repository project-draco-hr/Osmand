{
  super.onDraw(cv);
  if (isVisible()) {
    cv.drawText(text,3 * scaleCoefficient,getHeight() - scaleCoefficient,textPaint);
    if (subtext != null) {
      cv.drawText(subtext,7 * scaleCoefficient + textPaint.measureText(text),getHeight() - scaleCoefficient,subtextPaint);
    }
  }
}
