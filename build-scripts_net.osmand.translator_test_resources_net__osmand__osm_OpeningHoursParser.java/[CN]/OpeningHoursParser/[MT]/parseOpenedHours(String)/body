{
  String[] rules=format.split(";");
  OpeningHours rs=new OpeningHours();
  for (  String r : rules) {
    r=r.trim();
    if (r.length() == 0) {
      continue;
    }
    boolean rule=parseRule(r,rs);
    if (!rule) {
      return null;
    }
  }
  return rs;
}
