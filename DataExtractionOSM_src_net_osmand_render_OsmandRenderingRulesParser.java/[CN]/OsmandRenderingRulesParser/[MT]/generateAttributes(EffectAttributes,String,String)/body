{
  if (s.color != 0) {
    res+=" color" + prefix + "="+ colorToString(s.color);
  }
  if (s.strokeWidth != 0) {
    res+=" strokeWidth" + prefix + "="+ s.strokeWidth;
  }
  if (s.pathEffect != null) {
    res+=" pathEffect" + prefix + "="+ s.pathEffect;
  }
  return res;
}
