{
  if (direction >= 0) {
    if (forwardWays == null) {
      forwardWays=new ArrayList<>();
    }
    forwardWays.add(w);
  }
  if (direction <= 0) {
    if (backwardWays == null) {
      backwardWays=new ArrayList<>();
    }
    backwardWays.add(w);
  }
}
