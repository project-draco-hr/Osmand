{
  BinaryInspector in=new BinaryInspector();
  if (args.length == 1 && "test".equals(args[0])) {
    in.inspector(new String[]{"-vmap","-vmapobjects","/Users/victorshcherb/osmand/maps/Synthetic_test_rendering.obf"});
  }
 else {
    in.inspector(args);
  }
}
