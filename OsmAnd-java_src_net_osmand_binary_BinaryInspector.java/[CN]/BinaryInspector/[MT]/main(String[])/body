{
  BinaryInspector in=new BinaryInspector();
  if (args.length == 1 && "test".equals(args[0])) {
    in.inspector(new String[]{"-vmap","-vstats","/Users/victorshcherb/osmand/maps/World_basemap_2.obf"});
  }
 else {
    in.inspector(args);
  }
}
