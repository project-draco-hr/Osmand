{
  BinaryInspector in=new BinaryInspector();
  if (args.length == 1 && "test".equals(args[0])) {
    in.inspector(new String[]{"/Users/victorshcherb/osmand/osm-gen/Andorra-latest.origin.obf"});
  }
 else {
    in.inspector(args);
  }
}
