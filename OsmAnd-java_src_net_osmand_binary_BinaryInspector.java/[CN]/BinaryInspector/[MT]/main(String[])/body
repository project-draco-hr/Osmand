{
  BinaryInspector in=new BinaryInspector();
  if (args == null || args.length == 0) {
    BinaryInspector.printUsage(null);
    return;
  }
  if ("test".equals(args[0])) {
    in.inspector(new String[]{System.getProperty("maps.dir") + "/Gb_england_europe_simple.obf"});
  }
 else {
    in.inspector(args);
  }
}
