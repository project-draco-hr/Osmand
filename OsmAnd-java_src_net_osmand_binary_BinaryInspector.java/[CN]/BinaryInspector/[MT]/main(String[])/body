{
  BinaryInspector in=new BinaryInspector();
  if (args.length == 1 && "test".equals(args[0])) {
    in.inspector(new String[]{"/home/victor/projects/osmand/osm-gen/Map.obf"});
  }
 else {
    in.inspector(args);
  }
}
