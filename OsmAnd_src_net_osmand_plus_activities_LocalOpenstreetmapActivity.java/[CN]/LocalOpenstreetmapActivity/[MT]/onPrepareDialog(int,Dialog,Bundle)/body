{
switch (id) {
case DIALOG_PROGRESS_UPLOAD:
    UploadOpenstreetmapPointAsyncTask uploadTask=new UploadOpenstreetmapPointAsyncTask(progressPointDlg,remote,toUpload.length);
  uploadTask.execute(toUpload);
break;
}
}
