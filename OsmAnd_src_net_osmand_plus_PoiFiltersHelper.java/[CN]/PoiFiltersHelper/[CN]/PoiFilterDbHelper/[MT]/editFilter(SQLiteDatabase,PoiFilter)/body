{
  if (db != null) {
    db.execSQL("DELETE FROM " + CATEGORIES_NAME + " WHERE "+ CATEGORIES_FILTER_ID+ " = ?",new Object[]{filter.getFilterId()});
    addFilter(filter,db,true);
    updateName(db,filter);
    return true;
  }
  return false;
}
