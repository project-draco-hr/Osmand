{
  this.name=name;
  if (depends != null && depends.length() > 0) {
    for (    String s : depends.split(",")) {
      if (s.trim().length() > 0) {
        this.depends.add(s.trim());
      }
    }
  }
}
