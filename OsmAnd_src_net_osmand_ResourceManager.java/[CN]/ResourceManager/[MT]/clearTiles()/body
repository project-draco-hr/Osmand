{
  log.info("Cleaning tiles - size = " + cacheOfImages.size());
  ArrayList<String> list=new ArrayList<String>(cacheOfImages.keySet());
  for (int i=0; i < list.size() / 2; i++) {
    cacheOfImages.remove(list.get(i));
  }
}
