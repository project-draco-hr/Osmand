{
  p.addBatch();
  if (pStatements.get(p) >= batchSize) {
    p.executeBatch();
    if (commit) {
      p.getConnection().commit();
    }
    pStatements.put(p,0);
  }
 else {
    pStatements.put(p,pStatements.get(p) + 1);
  }
}
