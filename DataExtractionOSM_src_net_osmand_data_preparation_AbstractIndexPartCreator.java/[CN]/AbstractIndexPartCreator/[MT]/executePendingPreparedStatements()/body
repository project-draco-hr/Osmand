{
  boolean exec=false;
  for (  PreparedStatement p : pStatements.keySet()) {
    if (pStatements.get(p) > 0) {
      p.executeBatch();
      pStatements.put(p,0);
      exec=true;
    }
  }
  return exec;
}
