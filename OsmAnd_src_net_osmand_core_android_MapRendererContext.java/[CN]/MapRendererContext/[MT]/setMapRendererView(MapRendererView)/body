{
synchronized (_syncObject) {
    boolean update=(_mapRendererView != mapRendererView);
    if (!update)     return;
    _mapRendererView=mapRendererView;
    if (_mapRendererView != null)     apply();
  }
}
