{
synchronized (_syncObject) {
    boolean update=(_languagePreference != languagePreference);
    if (!update)     return;
    _languagePreference=languagePreference;
    if (_mapPresentationEnvironment != null)     updateMapPresentationEnvironment();
  }
}
