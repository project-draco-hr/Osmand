{
synchronized (_syncObject) {
    boolean update=(_obfsCollection != obfsCollection);
    if (!update)     return;
    _obfsCollection=obfsCollection;
    if (_obfMapObjectsProvider != null)     updateObfMapObjectsProvider();
  }
}
