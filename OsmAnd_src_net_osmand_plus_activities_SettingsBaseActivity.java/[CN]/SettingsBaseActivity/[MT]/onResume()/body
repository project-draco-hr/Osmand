{
  super.onResume();
  if (profileSettings) {
    previousAppMode=settings.getApplicationMode();
    boolean found=setSelectedAppMode(previousAppMode);
    if (!found) {
    }
  }
 else {
    updateAllSettings();
  }
}
