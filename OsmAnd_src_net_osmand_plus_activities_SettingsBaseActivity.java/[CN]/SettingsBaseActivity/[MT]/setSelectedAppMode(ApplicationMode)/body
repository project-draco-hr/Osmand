{
  int ind=0;
  boolean found=false;
  for (  ApplicationMode a : modes) {
    if (am == a) {
      getSupportActionBar().setSelectedNavigationItem(ind);
      found=true;
      break;
    }
    ind++;
  }
  return found;
}
