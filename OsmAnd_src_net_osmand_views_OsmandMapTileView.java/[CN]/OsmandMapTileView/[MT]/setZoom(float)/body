{
  if ((map == null && zoom < 22 && zoom > 0) || (map != null && (map.getMaximumZoomSupported() + OVERZOOM_IN) >= zoom && map.getMinimumZoomSupported() <= zoom)) {
    animatedDraggingThread.stopAnimating();
    this.zoom=zoom;
    refreshMap();
  }
}
