{
  final Collator instance=Collator.getInstance();
  Collections.sort(list,new Comparator<PoiUIFilter>(){
    @Override public int compare(    PoiUIFilter lhs,    PoiUIFilter rhs){
      return instance.compare(lhs.getName(),rhs.getName());
    }
  }
);
}
