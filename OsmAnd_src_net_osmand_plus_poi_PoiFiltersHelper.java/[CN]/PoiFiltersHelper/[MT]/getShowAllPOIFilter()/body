{
  if (showAllPOIFilter == null) {
    PoiLegacyFilter filter=new PoiLegacyFilter(null,application);
    filter.setStandardFilter(true);
    showAllPOIFilter=filter;
  }
  return showAllPOIFilter;
}
