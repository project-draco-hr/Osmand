{
  super.onDraw(cv);
  if (isVisible()) {
    int margin=0;
    if (imageDrawable != null) {
      imageDrawable.draw(cv);
      margin=(int)(imageDrawable.getBounds().width() + 2 * scaleCoefficient);
    }
    margin+=leftMargin;
    drawShadowText(cv,text,margin,getWHeight() - 3 * scaleCoefficient,textPaint);
    if (subtext != null) {
      drawShadowText(cv,subtext,margin + 2 * scaleCoefficient + textPaint.measureText(text),getWHeight() - 3 * scaleCoefficient,subtextPaint);
    }
  }
}
