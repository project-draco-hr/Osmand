{
  Map<String,String> files=DownloaderIndexFromGoogleCode.getContent(new LinkedHashMap<String,String>(),".zip");
  for (  String s : files.keySet()) {
    System.out.println(s + " " + files.get(s));
  }
}
