{
  List<Node> points=this.collectPoints();
  for (  Node n : points) {
    if (!r.containsNode(n)) {
      return false;
    }
  }
  points=r.collectPoints();
  for (  Node n : points) {
    if (this.containsNode(n)) {
      return false;
    }
  }
  return true;
}
