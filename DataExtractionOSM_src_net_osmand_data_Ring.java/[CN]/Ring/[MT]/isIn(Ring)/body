{
  List<Node> points=this.getBorder();
  for (  Node n : points) {
    if (!r.containsNode(n)) {
      return false;
    }
  }
  points=r.getBorder();
  for (  Node n : points) {
    if (this.containsNode(n)) {
      return false;
    }
  }
  return true;
}
