{
  if (mActionMode != null) {
    mActionMode.finish();
    return true;
  }
  ActionBar ab=getSupportActionBar();
  if (ab != null && ab.collapseActionView()) {
    return true;
  }
  return false;
}
