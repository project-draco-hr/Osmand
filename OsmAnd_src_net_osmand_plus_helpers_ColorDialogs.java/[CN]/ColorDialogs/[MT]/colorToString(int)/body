{
  if ((0xFF000000 & color) == 0xFF000000) {
    return "#" + Integer.toHexString(color & 0x00FFFFFF);
  }
 else {
    private final static String ZEROES="000000";
    String c=Integer.toHexString(color);
    return "#" + c.length() <= 6 ? ZEROES.substring(c.length()) + c : c;
  }
}
