{
  if ((0xFF000000 & color) == 0xFF000000) {
    String c=Integer.toHexString(color & 0x00FFFFFF);
    return "#" + c.length() <= 6 ? "000000".substring(c.length()) + c : c;
  }
 else {
    return "#" + Integer.toHexString(color);
  }
}
