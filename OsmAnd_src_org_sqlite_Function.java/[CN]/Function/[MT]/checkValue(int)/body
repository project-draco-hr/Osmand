{
  if (conn == null || conn.db() == null || value == 0)   throw new SQLException("not in value access state");
  if (arg >= args)   throw new SQLException("arg " + arg + " out bounds [0,"+ args+ ")");
}
