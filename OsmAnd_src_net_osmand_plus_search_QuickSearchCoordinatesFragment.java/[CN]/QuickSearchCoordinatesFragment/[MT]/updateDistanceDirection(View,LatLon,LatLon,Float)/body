{
  TextView distanceText=(TextView)view.findViewById(R.id.distance);
  ImageView direction=(ImageView)view.findViewById(R.id.direction);
  DashLocationFragment.updateLocationView(useMapCenter,myLatLon,heading,direction,distanceText,location.getLatitude(),location.getLongitude(),screenOrientation,getMyApplication(),getMapActivity());
}
