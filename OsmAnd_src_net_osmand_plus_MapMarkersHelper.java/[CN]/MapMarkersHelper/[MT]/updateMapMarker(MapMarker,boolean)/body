{
  if (marker != null) {
    settings.updateMapMarker(marker.getLatitude(),marker.getLongitude(),marker.pointDescription,marker.colorIndex,marker.pos,marker.selected);
    if (refresh) {
      readFromSettings();
      refresh();
    }
  }
}
