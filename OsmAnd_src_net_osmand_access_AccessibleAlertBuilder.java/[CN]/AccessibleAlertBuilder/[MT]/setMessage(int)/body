{
  if (((OsmandApplication)context.getApplicationContext()).getSettings().ACCESSIBILITY_EXTENSIONS.get())   return setView(TextMessage.makeView(context,msgid,R.layout.alert));
  return super.setMessage(msgid);
}
