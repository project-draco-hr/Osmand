{
  List<AbstractPoiType> tm=new ArrayList<AbstractPoiType>();
  for (  PoiType pt : pf.getPoiTypes()) {
    if (pt.isReference()) {
      continue;
    }
    addIf(tm,pt,matcher);
  }
  return tm;
}
