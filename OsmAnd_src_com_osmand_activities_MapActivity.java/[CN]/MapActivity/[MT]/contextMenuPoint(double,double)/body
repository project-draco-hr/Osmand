{
  Builder builder=new AlertDialog.Builder(this);
  Resources resources=this.getResources();
  builder.setItems(new String[]{resources.getString(R.string.context_menu_item_navigate_point),resources.getString(R.string.context_menu_item_add_favorite),resources.getString(R.string.context_menu_item_update_map),resources.getString(R.string.context_menu_item_open_bug),resources.getString(R.string.context_menu_item_create_poi)},new DialogInterface.OnClickListener(){
    @Override public void onClick(    DialogInterface dialog,    int which){
      if (which == 0) {
        navigateToPoint(new LatLon(latitude,longitude));
      }
 else       if (which == 1) {
        addFavouritePoint(latitude,longitude);
      }
 else       if (which == 2) {
        reloadTile(mapView.getZoom(),latitude,longitude);
      }
 else       if (which == 3) {
        osmBugsLayer.openBug(MapActivity.this,getLayoutInflater(),mapView,latitude,longitude);
      }
 else       if (which == 4) {
        EditingPOIActivity activity=new EditingPOIActivity(MapActivity.this);
        activity.showCreateDialog(latitude,longitude);
      }
    }
  }
);
  builder.create().show();
}
