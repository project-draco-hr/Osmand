{
  if (globalPreferences.contains(LAST_SEARCHED_LAT) && globalPreferences.contains(LAST_SEARCHED_LON)) {
    return new LatLon(globalPreferences.getFloat(LAST_SEARCHED_LAT,0),globalPreferences.getFloat(LAST_SEARCHED_LON,0));
  }
  return null;
}
