{
  stopAnimatingSync();
  if (zoomStart < zoomEnd) {
    dirZ=1;
  }
 else {
    dirZ=-1;
  }
  curZ=zoomStart;
  endZ=zoomEnd;
  timeZ=600;
  time=System.currentTimeMillis();
  stopped=false;
  Thread thread=new Thread(this,"Animatable dragging");
  thread.start();
}
