{
  if (isInEdit) {
    throw new IllegalStateException("Can't modify in edit mode");
  }
  try {
    isInEdit=true;
    tagValues.remove(tag);
    notifyDatasetChanged();
  }
  finally {
    isInEdit=false;
  }
}
