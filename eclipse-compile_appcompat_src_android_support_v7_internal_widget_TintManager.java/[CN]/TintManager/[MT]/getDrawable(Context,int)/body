{
  if (isInTintList(resId)) {
    final TintManager tm=(context instanceof TintContextWrapper) ? ((TintContextWrapper)context).getTintManager() : new TintManager(context);
    return tm.getDrawable(resId);
  }
 else {
    return ContextCompat.getDrawable(context,resId);
  }
}
