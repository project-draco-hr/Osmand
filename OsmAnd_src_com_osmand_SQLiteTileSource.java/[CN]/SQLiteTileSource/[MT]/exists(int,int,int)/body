{
  Cursor cursor=getDatabase().rawQuery("SELECT 1 FROM tiles WHERE x = ? AND y = ? AND z = ?",new String[]{x + "",y + "",(17 - zoom) + ""});
  boolean e=cursor.moveToFirst();
  cursor.close();
  return e;
}
