def listsplit(tosplit, n):
    splited = []
    i = 0
    while (i < (len(tosplit) - n)):
        splited.append(tosplit[i:(i + n)])
        i += n
    last = tosplit[i:]
    last.append(tosplit[0])
    splited.append(last)
    return splited
