{
  Class<?> delegateClass=originalBinding.getClass();
  newDeclaringClass=Types.getRenamedBinding(newDeclaringClass);
  return (ITypeBinding)Proxy.newProxyInstance(delegateClass.getClassLoader(),delegateClass.getInterfaces(),new RenamedTypeBinding(newName,newDeclaringClass,originalBinding,modifiers));
}
