{
  getSerializedSize();
  if (hasLeft()) {
    output.writeSInt64(1,getLeft());
  }
  if (hasRight()) {
    output.writeSInt64(2,getRight());
  }
  if (hasTop()) {
    output.writeSInt64(3,getTop());
  }
  if (hasBottom()) {
    output.writeSInt64(4,getBottom());
  }
  getUnknownFields().writeTo(output);
}
