{
  while (hasOverflowed()) {
    final Node<K,V> node=evictionDeque.poll();
    if (node == null) {
      return;
    }
    if (data.remove(node.key,node)) {
      pendingNotifications.add(node);
    }
    makeDead(node);
  }
}
