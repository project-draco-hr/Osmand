{
  if (latLon == null) {
    return;
  }
  isNew=true;
  favorite=new FavouritePoint(latLon.getLatitude(),latLon.getLongitude(),title,app.getSettings().LAST_FAV_CATEGORY_ENTERED.get());
  favorite.setDescription("");
  FavoritePointEditorFragment.showInstance(mapActivity);
}
