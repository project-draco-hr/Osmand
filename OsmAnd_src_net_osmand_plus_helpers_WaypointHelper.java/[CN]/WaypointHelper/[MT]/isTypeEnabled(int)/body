{
  if (type == ALARMS) {
    return showAlarms();
  }
 else   if (type == POI) {
    return showPOI();
  }
 else   if (type == FAVORITES) {
    return showFavorites();
  }
 else   if (type == WAYPOINTS) {
    return showGPXWaypoints();
  }
  return true;
}
