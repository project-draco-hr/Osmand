{
  garbageCollectorIteration++;
  int loadedTilesCritical=config.NUMBER_OF_DESIRABLE_TILES_IN_MEMORY * 3 / 2;
  if (garbageCollectorIteration > config.ITERATIONS_TO_RUN_GC || getCurrentlyLoadedTiles() > loadedTilesCritical) {
    garbageCollectorIteration=0;
    return true;
  }
  return false;
}
