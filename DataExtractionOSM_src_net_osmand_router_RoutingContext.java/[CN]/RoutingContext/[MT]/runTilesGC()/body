{
  garbageCollectorIteration++;
  int loadedTilesCritical=NUMBER_OF_DESIRABLE_TILES_IN_MEMORY * 3 / 2;
  if (garbageCollectorIteration > ITERATIONS_TO_RUN_GC || getCurrentlyLoadedTiles() > loadedTilesCritical) {
    garbageCollectorIteration=0;
    return true;
  }
  return false;
}
