{
  if (zoom <= 14) {
    int d=(int)(1 * (1 << (14 - zoom)));
    return " < " + d + " "+ Messages.getMessage(Messages.KEY_KM);
  }
 else {
    return " < 500 " + Messages.getMessage(Messages.KEY_M);
  }
}
