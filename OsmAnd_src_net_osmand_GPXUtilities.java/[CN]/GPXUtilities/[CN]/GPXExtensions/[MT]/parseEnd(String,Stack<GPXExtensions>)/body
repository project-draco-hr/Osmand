{
  if (tag.equals("extensions") && extensionReadMode) {
    extensionReadMode=false;
    return true;
  }
 else {
    if (isEndingTag(tag)) {
      parserState.pop();
      return true;
    }
 else {
      return false;
    }
  }
}
