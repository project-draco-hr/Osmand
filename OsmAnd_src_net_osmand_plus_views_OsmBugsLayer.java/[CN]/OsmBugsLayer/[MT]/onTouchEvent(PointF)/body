{
  OpenStreetBug bug=getBugFromPoint(point);
  if (bug != null) {
    String format=view.getContext().getString(R.string.osb_bug_name) + " : " + bug.getName();
    Toast.makeText(view.getContext(),format,Toast.LENGTH_LONG).show();
    return true;
  }
  return false;
}
