{
  OpenStreetBug bug=getBugFromPoint(point);
  if (bug != null) {
    String format=activity.getString(R.string.osb_bug_name) + " : " + bug.getName();
    Toast.makeText(activity,format,Toast.LENGTH_LONG).show();
    return true;
  }
  return false;
}
