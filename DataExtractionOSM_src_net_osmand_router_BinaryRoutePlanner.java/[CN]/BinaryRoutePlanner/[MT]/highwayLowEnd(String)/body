{
  if (highway == null || highway.endsWith("_link") || highway.endsWith("services") || highway.endsWith("service") || highway.endsWith("unclassified") || highway.endsWith("road")) {
    return true;
  }
  return false;
}
