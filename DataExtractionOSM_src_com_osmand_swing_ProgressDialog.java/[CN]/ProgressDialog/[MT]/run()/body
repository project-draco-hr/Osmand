{
  finished=false;
  result=null;
  new WorkerThread().start();
synchronized (this) {
    if (!finished) {
      setVisible(true);
    }
  }
  if (exception != null) {
    throw exception;
  }
  return result;
}
