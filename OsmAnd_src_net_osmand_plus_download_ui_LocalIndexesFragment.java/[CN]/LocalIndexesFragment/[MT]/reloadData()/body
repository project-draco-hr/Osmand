{
  LoadLocalIndexTask current=asyncLoader;
  if (current == null || current.getStatus() == Status.FINISHED || current.isCancelled() || current.getResult() != null) {
    asyncLoader=new LoadLocalIndexTask();
    asyncLoader.execute(getActivity());
  }
}
