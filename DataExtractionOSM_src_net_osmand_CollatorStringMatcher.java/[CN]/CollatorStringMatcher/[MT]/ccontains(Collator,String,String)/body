{
  int pos=0;
  if (part.length() > 3) {
    pos=cindexOf(collator,pos,part.substring(0,3),base);
    if (pos == -1) {
      return false;
    }
  }
  pos=cindexOf(collator,pos,part,base);
  if (pos == -1) {
    return false;
  }
  return true;
}
