{
  PointF point=startDragging;
  if (point != null) {
    startDragging=null;
    for (int i=layers.size() - 1; i >= 0; i--) {
      if (layers.get(i).onTouchEvent(point)) {
        return;
      }
    }
  }
}
