{
  if (i == null) {
    return "";
  }
  if (!Algorithms.isEmpty(i.getRef())) {
    return i.getRef();
  }
 else   if (!Algorithms.isEmpty(i.getStreetName())) {
    return i.getStreetName();
  }
  return "";
}
