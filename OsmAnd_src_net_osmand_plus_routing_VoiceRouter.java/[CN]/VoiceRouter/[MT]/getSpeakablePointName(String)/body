{
  if (pn != null) {
    pn=pn.replace('-',' ');
    pn=pn.replace(':',' ');
    if (!AbstractPrologCommandPlayer.getLanguage().equals("de")) {
      pn=pn.replace("\u00df","ss");
    }
  }
  return pn;
}
