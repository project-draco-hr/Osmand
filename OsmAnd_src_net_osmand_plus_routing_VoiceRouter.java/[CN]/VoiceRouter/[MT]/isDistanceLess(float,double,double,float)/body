{
  if (defSpeed <= 0) {
    defSpeed=DEFAULT_SPEED;
  }
  if (currentSpeed <= 0) {
    currentSpeed=DEFAULT_SPEED;
  }
  if (dist < etalon || ((dist / currentSpeed) < (etalon / defSpeed))) {
    return true;
  }
  return false;
}
