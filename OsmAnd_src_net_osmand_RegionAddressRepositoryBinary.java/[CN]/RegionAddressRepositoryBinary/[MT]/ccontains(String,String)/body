{
  int pos=0;
  if (part.length() > 3) {
    pos=cindexOf(pos,part.substring(0,3),base);
    if (pos == -1) {
      return false;
    }
  }
  pos=cindexOf(pos,part,base);
  if (pos == -1) {
    return false;
  }
  return true;
}
