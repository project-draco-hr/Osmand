{
  int missedTags=0;
  for (  String tag : TAGS) {
    if (!collectedTags.contains(tag)) {
      LOG.debug("tags missing '" + tag + "' tag");
      missedTags++;
    }
  }
  LOG.debug("missing " + missedTags + " tags");
}
