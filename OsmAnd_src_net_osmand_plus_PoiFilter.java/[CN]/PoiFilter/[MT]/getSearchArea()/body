{
  double val=distanceToSearchValues[distanceInd];
  if (val >= 1) {
    return " < " + ((int)val) + " "+ application.getString(R.string.km);
  }
 else {
    return " < 500 " + application.getString(R.string.m);
  }
}
