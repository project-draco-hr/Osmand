{
  double val=distanceToSearchValues[distanceInd];
  if (val >= 1) {
    return " < " + OsmAndFormatter.getFormattedDistance(((int)val * 1000),application);
  }
 else {
    return " < " + OsmAndFormatter.getFormattedDistance(500,application);
  }
}
