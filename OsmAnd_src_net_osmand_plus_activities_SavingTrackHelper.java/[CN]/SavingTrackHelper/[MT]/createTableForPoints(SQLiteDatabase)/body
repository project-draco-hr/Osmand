{
  try {
    db.execSQL("CREATE TABLE " + POINT_NAME + " ("+ POINT_COL_LAT+ " double, "+ POINT_COL_LON+ " double, "+ POINT_COL_DATE+ " long, "+ POINT_COL_DESCRIPTION+ " text)");
  }
 catch (  RuntimeException e) {
  }
}
