{
  getSerializedSize();
  for (  int element : getCategoriesList()) {
    output.writeUInt32(3,element);
  }
  for (  int element : getSubcategoriesList()) {
    output.writeUInt32(5,element);
  }
  getUnknownFields().writeTo(output);
}
