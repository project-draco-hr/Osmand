{
  if (result.hasIdTable() && result.idTable_ != net.osmand.binary.OsmandOdb.IdTable.getDefaultInstance()) {
    result.idTable_=net.osmand.binary.OsmandOdb.IdTable.newBuilder(result.idTable_).mergeFrom(value).buildPartial();
  }
 else {
    result.idTable_=value;
  }
  result.hasIdTable=true;
  return this;
}
