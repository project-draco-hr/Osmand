{
  Osmformat.StringTable.Builder builder=Osmformat.StringTable.newBuilder();
  builder.addS(ByteString.copyFromUtf8(""));
  for (int i=0; i < set.length; i++)   builder.addS(ByteString.copyFromUtf8(set[i]));
  return builder;
}
