{
  if (result.hasRoot() && result.root_ != net.osmand.binary.OsmandOdb.MapTree.getDefaultInstance()) {
    result.root_=net.osmand.binary.OsmandOdb.MapTree.newBuilder(result.root_).mergeFrom(value).buildPartial();
  }
 else {
    result.root_=value;
  }
  result.hasRoot=true;
  return this;
}
