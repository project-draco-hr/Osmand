{
  if (result.hasNameIndex() && result.nameIndex_ != net.osmand.binary.OsmandOdb.OsmAndAddressNameIndexData.getDefaultInstance()) {
    result.nameIndex_=net.osmand.binary.OsmandOdb.OsmAndAddressNameIndexData.newBuilder(result.nameIndex_).mergeFrom(value).buildPartial();
  }
 else {
    result.nameIndex_=value;
  }
  result.hasNameIndex=true;
  return this;
}
