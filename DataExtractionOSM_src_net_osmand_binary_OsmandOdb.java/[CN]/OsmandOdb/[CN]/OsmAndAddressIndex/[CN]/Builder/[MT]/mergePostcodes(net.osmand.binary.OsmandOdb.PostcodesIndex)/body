{
  if (result.hasPostcodes() && result.postcodes_ != net.osmand.binary.OsmandOdb.PostcodesIndex.getDefaultInstance()) {
    result.postcodes_=net.osmand.binary.OsmandOdb.PostcodesIndex.newBuilder(result.postcodes_).mergeFrom(value).buildPartial();
  }
 else {
    result.postcodes_=value;
  }
  result.hasPostcodes=true;
  return this;
}
