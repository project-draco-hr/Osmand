{
  if (result.hasIntersections() && result.intersections_ != net.osmand.binary.OsmandOdb.InteresectedStreets.getDefaultInstance()) {
    result.intersections_=net.osmand.binary.OsmandOdb.InteresectedStreets.newBuilder(result.intersections_).mergeFrom(value).buildPartial();
  }
 else {
    result.intersections_=value;
  }
  result.hasIntersections=true;
  return this;
}
