{
  getSerializedSize();
  for (  net.osmand.binary.OsmandOdb.OsmAndAddressNameIndexData.AddressNameIndexDataBlocks element : getBlocksList()) {
    output.writeMessage(3,element);
  }
  if (hasTable()) {
    output.writeMessage(4,getTable());
  }
  getUnknownFields().writeTo(output);
}
