{
  SortedSet<Ring> set=getOuterRings();
  for (  Ring r : set) {
    if (!r.isClosed()) {
      return false;
    }
  }
  set=getInnerRings();
  for (  Ring r : set) {
    if (!r.isClosed()) {
      return false;
    }
  }
  return true;
}
