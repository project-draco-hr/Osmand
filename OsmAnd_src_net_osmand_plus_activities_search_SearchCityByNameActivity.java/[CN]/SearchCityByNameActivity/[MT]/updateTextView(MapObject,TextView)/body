{
  LatLon l=obj.getLocation();
  if (getFilter().length() > 2 && location != null && l != null) {
    txt.setText(obj.getName(region.useEnglishNames()) + " - " + OsmAndFormatter.getFormattedDistance((int)MapUtils.getDistance(l,location),this));
  }
 else {
    txt.setText(obj.getName(region.useEnglishNames()));
  }
}
