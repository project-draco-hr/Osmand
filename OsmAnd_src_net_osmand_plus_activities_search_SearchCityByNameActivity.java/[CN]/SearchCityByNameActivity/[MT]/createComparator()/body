{
  final StringMatcherMode startsWith=CollatorStringMatcher.StringMatcherMode.CHECK_ONLY_STARTS_WITH;
  return new CityComparator(startsWith,getMyApplication().getSettings().MAP_PREFERRED_LOCALE.get(),getMyApplication().getSettings().MAP_TRANSLITERATE_NAMES.get());
}
