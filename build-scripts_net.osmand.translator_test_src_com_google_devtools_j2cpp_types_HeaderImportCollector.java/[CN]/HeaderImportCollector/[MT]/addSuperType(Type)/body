{
  ITypeBinding binding=type != null ? Types.getTypeBinding(type) : null;
  if (includeMainType || binding == null || mainTypeName == null || !mainTypeName.equals(NameTable.getFullName(binding))) {
    super.addSuperType(type);
  }
}
