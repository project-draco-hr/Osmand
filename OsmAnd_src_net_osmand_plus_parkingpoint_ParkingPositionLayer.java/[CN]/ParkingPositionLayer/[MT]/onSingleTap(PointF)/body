{
  if (isParkingPointVisible()) {
    StringBuilder res=new StringBuilder();
    res.append("The location of your parked car");
    AccessibleToast.makeText(view.getContext(),res.toString(),Toast.LENGTH_LONG).show();
    return true;
  }
  return false;
}
