{
  if (tour != null) {
    boolean current=customization.getSelectedStage() == stage;
    ((TourViewActivity)getSherlockActivity()).createMenuItem(menu,START,current ? R.string.continue_stage : R.string.start_stage,0,0,MenuItem.SHOW_AS_ACTION_IF_ROOM | MenuItem.SHOW_AS_ACTION_WITH_TEXT);
  }
}
