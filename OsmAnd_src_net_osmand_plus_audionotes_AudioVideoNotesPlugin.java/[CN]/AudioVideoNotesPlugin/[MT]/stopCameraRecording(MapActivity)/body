{
  if (mediaRec != null) {
    mediaRec.stop();
    mediaRec.release();
    mediaRec=null;
    indexFile(true,mediaRecFile);
    mediaRecFile=null;
    stopCamera();
  }
  if (recordControl != null) {
    setRecordListener(recordControl,mapActivity);
  }
}
