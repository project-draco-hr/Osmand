def b(v):
    if PYTHON3:
        if isinstance(v, str):
            return v.encode('utf-8')
        elif isinstance(v, bytes):
            return v
        else:
            raise Exception('Unknown input type')
    else:
        return v
