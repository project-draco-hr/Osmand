{
  for (  WeakReference<Fragment> ref : fragSet) {
    Fragment f=ref.get();
    if (f != null)     if (f.isAdded()) {
      if (f instanceof DataSetChangedListener) {
        ((DataSetChangedListener)f).notifyDataSetChanged();
      }
    }
  }
}
