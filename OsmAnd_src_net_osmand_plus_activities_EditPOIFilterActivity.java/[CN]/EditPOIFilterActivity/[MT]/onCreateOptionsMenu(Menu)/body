{
  if (filter == null) {
    return super.onCreateOptionsMenu(menu);
  }
  createMenuItem(menu,FILTER,R.string.filter_current_poiButton,R.drawable.ic_action_done,MenuItemCompat.SHOW_AS_ACTION_WITH_TEXT | MenuItemCompat.SHOW_AS_ACTION_ALWAYS);
  return super.onCreateOptionsMenu(menu);
}
