{
  if (f == null) {
    return false;
  }
  if (f.isDirectory()) {
    for (    File c : f.listFiles()) {
      removeAllFiles(c);
    }
    return f.delete();
  }
 else {
    return f.delete();
  }
}
