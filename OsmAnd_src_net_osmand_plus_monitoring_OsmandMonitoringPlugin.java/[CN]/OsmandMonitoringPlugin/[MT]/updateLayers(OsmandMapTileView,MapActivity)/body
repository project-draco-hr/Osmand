{
  if ((!settings.SAVE_TRACK_TO_GPX.get()) && (mapView.getLayers().contains(monitoringLayer))) {
    mapView.removeLayer(monitoringLayer);
  }
 else {
    if (monitoringLayer == null)     registerLayers(activity);
    mapView.addLayer(monitoringLayer,16);
  }
}
