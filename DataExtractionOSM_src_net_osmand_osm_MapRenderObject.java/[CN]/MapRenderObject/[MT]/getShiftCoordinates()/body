{
  int shift=0;
  if (multitype) {
    shift=data[0] * 2 + 1;
    if (highwayType) {
      shift+=data[1] * 8 + 1;
    }
  }
 else   if (highwayType) {
    shift=data[0] * 8 + 1;
  }
  return shift;
}
