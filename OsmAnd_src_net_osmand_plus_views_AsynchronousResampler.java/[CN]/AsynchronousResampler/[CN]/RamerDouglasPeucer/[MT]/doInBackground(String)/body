{
  points=rs.getPoints();
  int nsize=points.size();
  survivor=new boolean[nsize];
  if (nsize > 0) {
    cullRamerDouglasPeucer(0,nsize - 1);
    survivor[0]=true;
  }
  culled=new ArrayList<>();
  for (int i=0; i < survivor.length; i++)   if (survivor[i])   culled.add(points.get(i));
  return "OK";
}
