{
  AlertDialog dlg=new AlertDialog.Builder(getActivity()).setMessage("Stage is completed TODO").setPositiveButton(R.string.default_buttons_ok,new DialogInterface.OnClickListener(){
    public void onClick(    DialogInterface dialog,    int whichButton){
    }
  }
).create();
  return dlg;
}
