{
  double multiple=MapUtils.convert31XToMeters(xB,xA,yA) * MapUtils.convert31XToMeters(xC,xA,yA) + MapUtils.convert31YToMeters(yB,yA,xA) * MapUtils.convert31YToMeters(yC,yA,xA);
  return multiple;
}
