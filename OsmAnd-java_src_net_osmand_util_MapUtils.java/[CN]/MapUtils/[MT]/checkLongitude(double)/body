{
  if (longitude > -180 && longitude <= 180) {
    return longitude;
  }
  while (longitude < -180 || longitude > 180) {
    if (longitude < 0) {
      longitude+=360;
    }
 else {
      longitude-=360;
    }
  }
  return longitude;
}
