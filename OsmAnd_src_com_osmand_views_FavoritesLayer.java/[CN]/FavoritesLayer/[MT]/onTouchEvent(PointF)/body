{
  FavouritePoint fav=getFavoriteFromPoint(point);
  if (fav != null) {
    String format=view.getContext().getString(R.string.favorite) + " : " + fav.getName();
    Toast.makeText(view.getContext(),format,Toast.LENGTH_LONG).show();
    return true;
  }
  return false;
}
