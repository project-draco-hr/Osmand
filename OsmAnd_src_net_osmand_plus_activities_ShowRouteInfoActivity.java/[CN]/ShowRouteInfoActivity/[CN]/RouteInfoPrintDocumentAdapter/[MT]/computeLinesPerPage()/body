{
  int linesPerPage=0;
  if (pageSize[1] > 0) {
    float height=pageSize[1];
    height-=marginTop;
    height-=marginBottom;
    linesPerPage=(int)(height / PDF_FONT_SIZE + PDF_TEXT_LEADING);
  }
  return linesPerPage;
}
