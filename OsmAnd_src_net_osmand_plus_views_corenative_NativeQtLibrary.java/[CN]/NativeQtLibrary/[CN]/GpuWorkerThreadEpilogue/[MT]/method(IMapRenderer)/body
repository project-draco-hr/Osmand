{
  try {
    if (!_egl.eglWaitGL())     Log.e(NATIVE_TAG,"Failed to wait for GPU worker context: " + _egl.eglGetError());
  }
 catch (  Exception e) {
    Log.e(NATIVE_TAG,"Failed to wait for GPU worker context",e);
  }
}
