{
  OsmandSettings.CommonPreference<Boolean> preference=preferenceForLocalIndex(LIVE_UPDATES_ON_POSTFIX,info);
  if (preference.get()) {
    dataShouldUpdate.add(info);
  }
 else {
    dataShouldNotUpdate.add(info);
  }
}
