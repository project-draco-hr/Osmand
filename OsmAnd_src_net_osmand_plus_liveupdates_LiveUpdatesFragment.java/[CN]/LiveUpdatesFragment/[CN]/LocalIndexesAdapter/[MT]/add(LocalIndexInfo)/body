{
  OsmandSettings.CommonPreference<Boolean> preference=preferenceLiveUpdatesOn(info,getSettings());
  if (preference.get()) {
    dataShouldUpdate.add(info);
  }
 else {
    dataShouldNotUpdate.add(info);
  }
}
