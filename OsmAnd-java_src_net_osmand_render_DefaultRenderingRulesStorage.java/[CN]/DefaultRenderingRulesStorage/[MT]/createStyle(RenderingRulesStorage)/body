{
  this.storage=storage;
  storage.renderingName="default";
  storage.internalRenderingName="default";
  initDictionary();
  initProperties();
  initConstants();
  initAttributes();
  initRules();
}
