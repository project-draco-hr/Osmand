{
  dirWithTiles=OsmandApplication.getSettings().extendOsmandPath(TILES_PATH);
  dirWithTiles.mkdirs();
  try {
    OsmandApplication.getSettings().extendOsmandPath(TILES_PATH + ".nomedia").createNewFile();
  }
 catch (  Exception e) {
  }
}
