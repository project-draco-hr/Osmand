{
  dirWithTiles=context.getSettings().extendOsmandPath(TILES_PATH);
  dirWithTiles.mkdirs();
  try {
    context.getSettings().extendOsmandPath(TILES_PATH + ".nomedia").createNewFile();
  }
 catch (  Exception e) {
  }
}
