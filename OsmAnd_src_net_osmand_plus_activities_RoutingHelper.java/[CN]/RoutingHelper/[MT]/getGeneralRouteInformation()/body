{
  int dist=getLeftDistance();
  int hours=getLeftTime() / (60 * 60);
  int minutes=(getLeftTime() / 60) % 60;
  return MessageFormat.format(context.getString(R.string.route_general_information),OsmAndFormatter.getFormattedDistance(dist,context),hours,minutes);
}
