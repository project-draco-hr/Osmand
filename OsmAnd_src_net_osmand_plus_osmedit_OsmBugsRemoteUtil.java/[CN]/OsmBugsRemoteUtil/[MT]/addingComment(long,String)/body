{
  StringBuilder b=new StringBuilder();
  b.append(getNotesApi()).append("/");
  b.append(id);
  b.append("/comment?text=").append(URLEncoder.encode(text));
  return editingPOI(b.toString(),"POST","adding comment");
}
