{
  StringBuilder b=new StringBuilder();
  b.append(getNotesApi()).append("/");
  b.append(id);
  b.append("/reopen?text=").append(URLEncoder.encode(text));
  return editingPOI(b.toString(),"POST","reopen bug");
}
