{
  StringBuilder b=new StringBuilder();
  b.append(SITE_API).append("/");
  b.append(id);
  b.append("/close?text=").append(URLEncoder.encode(text));
  return editingPOI(b.toString(),"POST","close bug");
}
