{
  if (topIdx < 0)   throw new StackUnderflowException("FileHdr.pop: Underflow");
  if (writeThr) {
    file.seek((Node.INTEGER_SIZE + Node.LONG_SIZE) + (Node.INTEGER_SIZE * topIdx));
    file.writeInt(Node.NOT_DEFINED);
  }
  dirty=true;
  return S[topIdx--];
}
