{
  List<List<WptPt>> tpoints=new ArrayList<List<WptPt>>();
  for (  Track t : tracks) {
    for (    TrkSegment ts : t.segments) {
      if (ts.points.size() > 0) {
        tpoints.add(ts.points);
      }
    }
  }
  return tpoints;
}
