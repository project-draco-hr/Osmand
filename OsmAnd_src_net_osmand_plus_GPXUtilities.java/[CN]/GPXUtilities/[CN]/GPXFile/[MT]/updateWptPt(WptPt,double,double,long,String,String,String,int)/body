{
  int index=points.indexOf(pt);
  pt.lat=lat;
  pt.lon=lon;
  pt.time=time;
  pt.desc=description;
  pt.name=name;
  pt.category=category;
  if (color != 0) {
    pt.setColor(color);
  }
  if (index != -1) {
    points.set(index,pt);
  }
}
