{
  String type=SpecialPhrases.getSpecialPhrase(amenity.getSubType());
  String n=amenity.getName(en);
  if (n.indexOf(type) != -1) {
    return n;
  }
  if (n.length() == 0) {
    return type;
  }
  return type + " " + n;
}
