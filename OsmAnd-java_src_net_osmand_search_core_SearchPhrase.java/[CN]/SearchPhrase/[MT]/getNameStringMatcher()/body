{
  if (sm != null) {
    return sm;
  }
  sm=new NameStringMatcher(unknownSearchWordTrim,(lastUnknownSearchWordComplete ? StringMatcherMode.CHECK_EQUALS_FROM_SPACE : StringMatcherMode.CHECK_STARTS_FROM_SPACE));
  return sm;
}
