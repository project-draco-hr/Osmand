def u(v):
    if PYTHON3:
        if isinstance(v, bytes):
            return v.decode('utf-8')
        elif isinstance(v, str):
            return v
        else:
            raise Exception('Unknown input type')
    else:
        return v
