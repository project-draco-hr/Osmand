{
  if (Build.VERSION.SDK_INT < Build.VERSION_CODES.JELLY_BEAN && mOriginalWindowCallback instanceof Activity) {
    return true;
  }
 else   if (mOriginalWindowCallback instanceof Dialog) {
    return true;
  }
  return false;
}
