{
  @DrawableRes int lst=items.get(position).getIcon();
  if (lst != -1) {
    return ctx.getResources().getDrawable(lst);
  }
  @DrawableRes int lstLight=items.get(position).getLightIcon();
  if (lstLight != -1) {
    return ctx.getIconsCache().getIcon(lstLight,light);
  }
  return null;
}
