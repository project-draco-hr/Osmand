{
  getMyApplication().getSettings().setMapLocationToShow(marker.getLatitude(),marker.getLongitude(),15,marker.getPointDescription(this),true,marker);
  MapActivity.launchMapActivityMoveToTop(this);
}
