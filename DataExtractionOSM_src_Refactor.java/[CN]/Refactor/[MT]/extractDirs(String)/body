{
  List<File> result=new ArrayList<File>();
  for (  String drawable : drawables) {
    if (desc.contains(drawable)) {
      desc=desc.replace(drawable,"");
      result.add(new File(OSM_AND_RES,drawable));
    }
  }
  return result;
}
