{
  double ret=defVal;
  String value=a.getValue(name);
  if (value == null) {
    return defVal;
  }
  try {
    ret=Double.parseDouble(value);
  }
 catch (  NumberFormatException e) {
  }
  return ret;
}
