{
  if (lst.length > subInd && lst[subInd].equals(id)) {
    if (lst.length == subInd + 1) {
      return this;
    }
 else     if (groups != null) {
      return getSubGroupById(lst,subInd + 1);
    }
  }
  return null;
}
