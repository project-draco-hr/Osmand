{
  super(context,attrs);
  if (attrs != null) {
    TypedArray resolvedAttrs=context.getTheme().obtainStyledAttributes(attrs,R.styleable.OsmandWidgets,0,0);
    parseAttributes(resolvedAttrs);
    resolvedAttrs.recycle();
  }
}
