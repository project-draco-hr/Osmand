{
  super(context,attrs,defStyleAttr,defStyleRes);
  if (attrs != null) {
    TypedArray resolvedAttrs=context.getTheme().obtainStyledAttributes(attrs,R.styleable.OsmandWidgets,defStyleAttr,defStyleRes);
    parseAttributes(resolvedAttrs);
    resolvedAttrs.recycle();
  }
}
