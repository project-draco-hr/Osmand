{
  if (!resolvedAttributes.hasValue(R.styleable.TextViewEx_textAllCapsCompat)) {
    return;
  }
  boolean textAllCaps=resolvedAttributes.getBoolean(R.styleable.TextViewEx_textAllCapsCompat,false);
  if (!textAllCaps) {
    return;
  }
  setAllCapsCompat(target,true);
}
