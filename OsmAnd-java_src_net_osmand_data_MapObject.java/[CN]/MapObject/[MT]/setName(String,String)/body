{
  if (Algorithms.isEmpty(lang)) {
    setName(name);
  }
 else   if (lang.equals("en")) {
    setEnName(name);
  }
 else {
    if (names == null) {
      names=new HashMap<String,String>();
    }
    names.put(lang,name);
  }
}
